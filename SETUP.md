# Patricia Clinic - Setup Guide
## คู่มือการติดตั้งและใช้งานระบบ

---

## 📋 ข้อกำหนดเบื้องต้น (Prerequisites)

### Software Requirements
- **Node.js**: เวอร์ชัน 18.0 หรือสูงกว่า
- **npm** หรือ **yarn**: Package manager
- **Git**: สำหรับ version control
- **Code Editor**: แนะนำ VS Code

### Supabase Account
- สมัครบัญชี Supabase ที่ https://supabase.com
- สร้าง Project ใหม่
- เตรียม URL และ Anon Key

---

## 🚀 การติดตั้งระบบ (Installation)

### ขั้นตอนที่ 1: Clone Repository

```bash
git clone <repository-url>
cd patricia-clinic
```

### ขั้นตอนที่ 2: ติดตั้ง Dependencies

```bash
npm install
# หรือ
yarn install
```

### ขั้นตอนที่ 3: ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local` และเพิ่มค่าต่อไปนี้:

```bash
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

**หมายเหตุ**: ค่า URL และ Key สามารถหาได้จาก Supabase Dashboard > Settings > API

---

## 🗄️ ตั้งค่า Supabase Database

### ขั้นตอนที่ 1: สร้าง Tables

1. เข้าไปที่ Supabase Dashboard
2. ไปที่ SQL Editor
3. Copy โค้ดจากไฟล์ `supabase/schema.sql`
4. รันคำสั่ง SQL

หรือรันทีละส่วน:

#### สร้าง Tables
```sql
-- Copy และรันโค้ดจาก supabase/schema.sql
-- ส่วนที่ CREATE TABLE
```

#### สร้าง Indexes
```sql
-- Copy และรันโค้ดส่วน CREATE INDEX
```

#### เปิดใช้งาน RLS
```sql
-- Copy และรันโค้ดส่วน Row Level Security
```

#### สร้าง Triggers
```sql
-- Copy และรันโค้ดส่วน CREATE TRIGGER
```

#### Insert ข้อมูลตัวอย่าง
```sql
-- Copy และรันโค้ดส่วน Sample Data
```

### ขั้นตอนที่ 2: ตั้งค่า Storage

1. ไปที่ Storage > Create new bucket
2. สร้าง bucket ชื่อ `treatment-images`
3. ตั้งค่า Public access:
   - Allowed MIME types: `image/*`
   - Max file size: `5MB`
4. สร้าง Policy สำหรับ upload:

```sql
-- Policy สำหรับ upload รูปภาพ
CREATE POLICY "Allow authenticated users to upload"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (bucket_id = 'treatment-images');

-- Policy สำหรับอ่านรูปภาพ
CREATE POLICY "Allow public to view images"
ON storage.objects FOR SELECT
TO public
USING (bucket_id = 'treatment-images');
```

### ขั้นตอนที่ 3: ตั้งค่า Authentication

1. ไปที่ Authentication > Settings
2. เปิดใช้งาน Email Provider
3. ปิด Email Confirmation (สำหรับการทดสอบ)
4. สร้างผู้ใช้ทดสอบ:
   - ไปที่ Authentication > Users
   - คลิก "Add user"
   - กรอกอีเมลและรหัสผ่าน
   - คลิก "Create user"

---

## 🔧 การรันระบบ (Running the Application)

### Development Mode

```bash
npm run dev
# หรือ
yarn dev
```

เปิดเบราว์เซอร์ที่ http://localhost:3000

### Production Build

```bash
# Build
npm run build

# Start
npm start
```

---

## 👤 การเข้าใช้งานระบบครั้งแรก

### 1. เข้าสู่ระบบ

- URL: http://localhost:3000
- ระบบจะ redirect ไปหน้า Login อัตโนมัติ
- ใช้อีเมลและรหัสผ่านที่สร้างใน Supabase

### 2. เพิ่มข้อมูลพนักงาน

หลังจากเข้าสู่ระบบแล้ว:

1. ไปที่เมนู "พนักงาน"
2. คลิก "เพิ่มพนักงาน"
3. กรอกข้อมูลพนักงานคนแรก พร้อมกำหนดเป็น "ผู้ดูแลระบบ"
4. **สำคัญ**: เชื่อมโยง user_id กับผู้ใช้ที่สร้างใน Authentication

**หมายเหตุ**: สามารถเชื่อมโยง user_id ด้วยการ query ใน SQL Editor:

```sql
-- ดู user_id ของผู้ใช้ที่สร้าง
SELECT id, email FROM auth.users;

-- Update staff record
UPDATE staff 
SET user_id = 'user-id-from-above' 
WHERE email = 'your-email@example.com';
```

### 3. เริ่มใช้งาน

ตอนนี้คุณสามารถ:
- เพิ่มลูกค้า
- สร้างบริการ
- เพิ่มสินค้า
- สร้างนัดหมาย
- บันทึกการขาย

---

## 📱 การใช้งานแต่ละฟีเจอร์

### Dashboard
- แสดงภาพรวมของคลินิก
- ยอดขายรายวัน/เดือน
- จำนวนลูกค้า
- นัดหมายวันนี้

### ลูกค้า (CRM)
**เพิ่มลูกค้าใหม่:**
1. คลิก "เพิ่มลูกค้าใหม่"
2. กรอกข้อมูล (ชื่อ, เบอร์โทร เป็นฟิลด์บังคับ)
3. คลิก "บันทึก"

**ดูประวัติลูกค้า:**
1. คลิก "ดูรายละเอียด" บนการ์ดลูกค้า
2. ดูประวัติการรักษาและการซื้อ

### นัดหมาย
**สร้างนัดหมาย:**
1. คลิก "สร้างนัดหมาย"
2. เลือกลูกค้า
3. เลือกบริการ (ระบบจะคำนวณเวลาอัตโนมัติ)
4. เลือกพนักงาน/แพทย์
5. กำหนดวันและเวลา
6. คลิก "สร้างนัดหมาย"

**จัดการสถานะ:**
- รอยืนยัน → ยืนยันแล้ว
- ยืนยันแล้ว → เสร็จสิ้น
- สามารถยกเลิกได้ทุกเวลา

### บริการ
**เพิ่มบริการ:**
1. กรอกชื่อบริการ
2. กำหนดราคา
3. กำหนดระยะเวลา (นาที)
4. เลือกหมวดหมู่
5. ติ๊ก "เป็นแพ็กเกจ" ถ้าเป็นคอร์ส

### สินค้า
**เพิ่มสินค้า:**
1. กรอกชื่อสินค้า
2. กำหนดรหัสสินค้า (SKU) - ต้องไม่ซ้ำ
3. ใส่จำนวน
4. กำหนดจุดสั่งซื้อใหม่
5. ใส่ราคาทุนและราคาขาย

**การแจ้งเตือน:**
- ระบบจะแจ้งเตือนเมื่อสินค้าลดลงถึงจุดสั่งซื้อ

### ขายและรายงาน
**สร้างบิลขาย:**
1. คลิก "สร้างบิลขาย"
2. เลือกลูกค้า
3. เลือกพนักงาน
4. เพิ่มบริการหรือสินค้า
5. ปรับจำนวนตามต้องการ
6. เลือกช่องทางชำระเงิน
7. คลิก "สร้างบิลขาย"

**ดูรายงาน:**
- กรองตามช่วงวันที่
- ดูกราฟยอดขาย
- ดูค่าคอมมิชชันพนักงาน

### พนักงาน
**เพิ่มพนักงาน:**
1. กรอกข้อมูลพนักงาน
2. กำหนดอัตราค่าคอมมิชชัน
3. เลือกบทบาท:
   - **ผู้ดูแลระบบ**: สิทธิ์เต็ม
   - **แพทย์**: จัดการลูกค้าและนัดหมาย
   - **พนักงาน**: สิทธิ์พื้นฐาน

---

## 🔐 การจัดการสิทธิ์และความปลอดภัย

### Row Level Security (RLS)

ระบบใช้ RLS ของ Supabase ในการควบคุมสิทธิ์:

- **Admin**: เข้าถึงได้ทุกอย่าง
- **Doctor**: เข้าถึงข้อมูลลูกค้า, นัดหมาย, การรักษา
- **Staff**: เข้าถึงข้อมูลพื้นฐาน

### Best Practices
1. **ไม่แชร์ Supabase Keys**: เก็บ keys เป็นความลับ
2. **ใช้ Environment Variables**: ไม่ hard-code ค่าใน code
3. **อัพเดท Dependencies**: รักษาความปลอดภัย
4. **Backup Database**: สำรอง database เป็นประจำ

---

## 🐛 Troubleshooting (แก้ปัญหา)

### ปัญหา: ไม่สามารถเข้าสู่ระบบได้

**แก้ไข:**
1. ตรวจสอบว่าสร้างผู้ใช้ใน Supabase Authentication แล้ว
2. ตรวจสอบ Email และ Password ให้ถูกต้อง
3. ตรวจสอบ SUPABASE_URL และ SUPABASE_ANON_KEY

### ปัญหา: ไม่สามารถดูข้อมูลได้

**แก้ไข:**
1. ตรวจสอบว่ารัน SQL schema ครบถ้วน
2. ตรวจสอบ RLS Policies
3. ตรวจสอบว่า user มี staff record ที่เชื่อมโยงกัน

### ปัญหา: Error เมื่อสร้างบิลขาย

**แก้ไข:**
1. ตรวจสอบว่ามีลูกค้า บริการ/สินค้า และพนักงานในระบบ
2. ตรวจสอบ Triggers ในฐานข้อมูล
3. ดู Console log สำหรับข้อความ error

### ปัญหา: สินค้าไม่ลดสต็อกอัตโนมัติ

**แก้ไข:**
1. ตรวจสอบว่าสร้าง Trigger `trigger_update_product_quantity` แล้ว
2. ตรวจสอบว่า Function `update_product_quantity()` ทำงานได้

### ปัญหา: Cannot find module

**แก้ไข:**
```bash
# ลบ node_modules และ install ใหม่
rm -rf node_modules
rm package-lock.json
npm install
```

---

## 📊 ตัวอย่างข้อมูลทดสอบ

ระบบมาพร้อมข้อมูลตัวอย่าง (Sample Data) ประกอบด้วย:

### พนักงาน
- แพทริเซีย คลินิก (ผู้จัดการ)
- ดร.สมชาย ใจดี (แพทย์)
- สมหญิง รักงาน (พนักงาน)

### บริการ
- ฉีดโบท็อกซ์ - ฿5,000
- ฉีดฟิลเลอร์ - ฿8,000
- เลเซอร์หน้าใส - ฿3,500
- แพ็กเกจดูแลผิว VIP - ฿25,000

### สินค้า
- เซรั่มวิตามินซี - ฿890
- ครีมกันแดด SPF50 - ฿690
- มาส์กหน้า - ฿150

### ลูกค้า
- คุณสมศรี ใจดี
- คุณวิภา สวยงาม

---

## 🚀 การ Deploy

### Deploy ไปยัง Vercel

1. Push code ไปยัง GitHub
2. เข้า https://vercel.com
3. Import repository
4. ตั้งค่า Environment Variables:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
5. คลิก Deploy

### Deploy ไปยัง Netlify

1. Push code ไปยัง GitHub
2. เข้า https://netlify.com
3. Import repository
4. Build command: `npm run build`
5. Publish directory: `.next`
6. ตั้งค่า Environment Variables
7. Deploy

---

## 📚 เอกสารเพิ่มเติม

- [Next.js Documentation](https://nextjs.org/docs)
- [Supabase Documentation](https://supabase.com/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)

---

## 🆘 การขอความช่วยเหลือ

หากพบปัญหาหรือต้องการความช่วยเหลือ:

1. ตรวจสอบ Documentation นี้อีกครั้ง
2. ดู Console log และ Error messages
3. ตรวจสอบ Supabase Dashboard > Logs
4. ดู Issues ใน GitHub repository

---

## ✅ Checklist การติดตั้ง

- [ ] ติดตั้ง Node.js และ npm
- [ ] Clone repository
- [ ] ติดตั้ง dependencies (`npm install`)
- [ ] สร้าง Supabase project
- [ ] ตั้งค่า `.env.local`
- [ ] รัน SQL schema
- [ ] สร้าง Storage bucket
- [ ] ตั้งค่า Authentication
- [ ] สร้างผู้ใช้ทดสอบ
- [ ] รันระบบ (`npm run dev`)
- [ ] เข้าสู่ระบบสำเร็จ
- [ ] เพิ่มข้อมูลพนักงาน
- [ ] ทดสอบฟีเจอร์ต่างๆ

---

**ขอให้ใช้งานระบบอย่างมีประสิทธิภาพ!** 🎉

**สร้างโดย**: Patricia Clinic Development Team  
**เวอร์ชัน**: 1.0.0  
**อัพเดทล่าสุด**: 2025-10-06
